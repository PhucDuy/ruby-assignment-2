    <nav class="navbar navbar-expand-md  navbar-light bg-faded">
  <!-- Toggle Button -->
  <button class="navbar-toggler navbar-toggler-right" 
        type="button" 
        data-toggle="collapse" 
        data-target="#navbarSupportedContent" 
        aria-controls="navbarSupportedContent"
        aria-expanded="false" 
        aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <!-- Nav Content -->
  <div class="collapse navbar-collapse" id="nav-content">
    <a class="navbar-brand" href="<%= current_user.present? ? messages_path : root_path %>">SimpleChat</a>
    <ul class="nav navbar-nav">
      <% if current_user.present? %>
        <li class="nav-item">
          <%= link_to "Sign Out", log_out_path, method: 'delete', class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= messages_path %>">Browse messages</a>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_user_path %>">Sign Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_session_path %>">Sign In</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>